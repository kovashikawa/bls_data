# -----------------------------------------------------------------
# Requirements for BLS (Bureau of Labor Statistics) Data Fetcher
# -----------------------------------------------------------------
# Use this file to install the necessary dependencies:
# pip install -r requirements.txt
# -----------------------------------------------------------------

requests==2.31.0
pandas==2.2.2
python-dotenv==1.0.1

# Database requirements
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0